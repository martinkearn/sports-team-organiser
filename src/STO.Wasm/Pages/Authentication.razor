@page "/authentication/{action}"
@layout MainLayout
@inject NavigationManager Navigation
@using Microsoft.AspNetCore.Components.WebAssembly.Authentication
<RemoteAuthenticatorView Action="@Action" OnLogInSucceeded="@HandleNavigation" />

@code{
    [Parameter] public string? Action { get; set; }

    private async Task HandleNavigation()
    {
        //ToDo: This is working bu getting overridden later by the returnurl inside RemoteAuthenticatorView. Need to work out where that is being set
        Navigation.NavigateTo("/admin");
    }
}
