@if (!string.IsNullOrEmpty(css))
{
    <i class="@css"></i>
}
else
{
    @Position.ToString()
}


@code {
    [Parameter]
    public Enums.PlayerPosition Position { get; set; }

    private string css;

    protected override void OnParametersSet()
    {
        SetCss();
    }

    protected override void OnInitialized()
    {
        SetCss();
    }

    private void SetCss()
    {
        switch (Position)
        {
            case Enums.PlayerPosition.Defender:
                css = "fa-solid fa-shield-halved";
                break;
            case Enums.PlayerPosition.Midfielder:
                css = "fa-solid fa-arrows-spin";
                break;
            case Enums.PlayerPosition.Forward:
                css = "fa-solid fa-bullseye";
                break;
            case Enums.PlayerPosition.Goalie:
                css = "fa-solid fa-hand-dots";
                break;           
            default:
                break;
        }
    }
}